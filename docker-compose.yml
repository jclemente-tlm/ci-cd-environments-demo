services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      APP_ENVIRONMENT: local-docker
      APP_VERSION: 0.0.0-local
      APP_COMMIT_SHA: local
      APP_BRANCH: local
      APP_DEPLOYED_AT: local
